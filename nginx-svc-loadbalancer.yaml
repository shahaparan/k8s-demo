apiVersion: v1
kind: Service	
metadata:
  name: service-loadbalancer
  labels:
    app: nginx-loadbalancer
spec:
  selector:
    app: nginx-loadbalancer
  type: LoadBalancer
  ports:
  - nodePort: 31112
    port: 80
    targetPort: 80
